<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f0f2f5 0%, #e0e4eb 100%);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center text-gray-900">
    <div class="p-8 bg-white rounded-xl shadow-2xl w-full max-w-lg border border-gray-200">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-3xl font-bold text-gray-800">Edit your post</h2>
            <a class="bg-red-600 rounded-full px-5 py-2 text-sm text-white font-semibold hover:bg-red-700 transition-colors" href="/logout">Log out</a>
        </div>
        
        <form action="/update/<%= post._id %>" method="post" autocomplete="off">
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg text-center text-sm animate-pulse mb-6"><%= error %></div>
            <% } %>
            
            <% if (typeof csrfToken !== 'undefined') { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>

            <label for="content" class="block mb-2 text-sm font-medium text-gray-700">Post Content</label>
            <textarea
                id="content"
                class="w-full h-32 block outline-none px-4 py-3 bg-gray-100 border border-gray-300 resize-none rounded-lg text-gray-800 placeholder-gray-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-colors"
                name="content"
                required
                minlength="1"
                maxlength="1000"
                aria-label="Edit post content"
            ><%= post.content %></textarea>

            <button
                type="submit"
                class="w-full mt-6 bg-gradient-to-r from-yellow-500 to-yellow-600 px-8 py-3 rounded-lg text-white font-bold text-lg shadow-lg hover:from-yellow-600 hover:to-yellow-700 transition-all duration-300 transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:ring-opacity-50"
            >
                Update Post
            </button>
        </form>
    </div>
</body>
</html>
