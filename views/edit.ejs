<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="m-3 p-1 flex justify-end">
        <a class="bg-red-700 rounded-lg px-4 py-2 m-3" href="/logout">log out</a>
    </div>
<!-- <h1  class="text-3xl font-bold mx-6 mb-2">Hello, </h1> -->
<h2 class="text-lg  mx-6 mb-3">Edit your post here </h2>
<form action="/update/<%= post._id %>" method="post" autocomplete="off">
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="text-red-500 mx-6 mb-2"><%= error %></div>
    <% } %>
    <!-- CSRF token placeholder, add server-side implementation if using csurf -->
    <% if (typeof csrfToken !== 'undefined') { %>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <% } %>
    <label for="content" class="mx-6 block mb-1">Post Content</label>
    <textarea id="content" class="w-1/3 block outline-none px-3 py-2 bg-transparent border-[1.4px] border-zinc-700 resize-none rounded-md mt-3 mx-6" name="content" required minlength="1" maxlength="1000" aria-label="Edit post content"><%= post.content %></textarea>
    <input class="hover:cursor-pointer mt-4 ml-6 mb-10 bg-yellow-600 px-4 py-1 rounded-lg" type="submit" value="Update">
</form>