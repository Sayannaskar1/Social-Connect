<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
<div class="m-3 p-1 flex justify-end">

        <a class="bg-red-700 rounded-lg px-4 py-2 m-3" href="/logout">log out</a>
    </div>
    <div class="flex"><div class="ml-6 rounded-lg w-12 h-12 overflow-hidden">
        <img class="w-full h-full" src="/images/upload/<%= user.profilepicture %>" alt="Profile picture">
        <!-- <a class="bg-red-900" href="/profile/upload">upload image</a> -->
    </div>
    <h1 class="text-3xl font-bold ml-2 mb-2">Hello, <%= user.name %> 👋🏼</h1></div>

<h2 class="text-lg mx-6 mb-3">You can create a new post</h2>
<form action="/post" method="post" autocomplete="off">
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="text-red-500 mx-6 mb-2"><%= error %></div>
    <% } %>
    <!-- CSRF token placeholder, add server-side implementation if using csurf -->
    <% if (typeof csrfToken !== 'undefined') { %>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <% } %>
    <label for="content" class="mx-6 block mb-1">Post Content</label>
    <textarea id="content" placeholder="What's on your mind?" class="w-1/3 block outline-none px-3 py-2 bg-transparent border-[1.4px] border-zinc-700 resize-none rounded-md mt-3 mx-6" name="content" required minlength="1" maxlength="1000" aria-label="Create post"></textarea>
    <input class="hover:cursor-pointer mt-4 ml-6 mb-10 bg-yellow-600 px-4 py-1 rounded-lg" type="submit" value="Post">
</form>

<h3 class="mt-7 ml-5 text-lg mb-3">Your Posts:</h3>
<div class="  flex flex-wrap" >
    <% user.posts.slice().reverse().forEach((post) => { %>
        <div class="ml-6 mb-2 w-1/4 tracking-tighter rounded-md bg-transparent border-2 border-zinc-700 p-3">
            <h4 class="mb-2 text-blue-700 p-1"><%= user.username %></h4>
            <p class="text-sm tracking-tighter"><%= post.content %></p>
            <p class="ml-1 relative top-3 left-1"><%= post.likes.length %> likes</p>
            <div class="btns flex p-2 justify-between-start">
                <% if (post.likes.indexOf(user._id) === -1) { %>
                    <a class="mr-4 text-blue-800" href="/like/<%= post._id %>">like</a>
                <% } else { %>
                    <a class="mr-4 text-blue-800" href="/like/<%= post._id %>">dislike</a>
                <% } %>
                <% if (String(post.user) === String(user._id)) { %>
                    <a class="text-yellow-600" href="/edit/<%= post._id %>">edit</a>
                <% } %>
            </div>
        </div>
    <% }) %>



</div>


</body>
</html>