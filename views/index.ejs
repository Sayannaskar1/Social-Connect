<!--  -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex flex-col items-center justify-center text-white font-sans">
    <h1 class="text-4xl font-bold mb-8 text-blue-400">Sign Up</h1>
    <div class="p-8 bg-zinc-800 rounded-lg shadow-xl w-full max-w-sm border border-zinc-700">
        <form class="flex flex-col gap-4" action="/create" method="post" autocomplete="off">
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="text-red-500 text-center mb-2"><%= error %></div>
            <% } %>
            <% if (typeof csrfToken !== 'undefined') { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>
            
            <div>
                <label for="name" class="block text-sm font-medium text-zinc-300 mb-1">Name</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="text" name="name" id="name" placeholder="Full Name" required minlength="2" maxlength="100">
            </div>
            
            <div>
                <label for="username" class="block text-sm font-medium text-zinc-300 mb-1">Username</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="text" name="username" id="username" placeholder="username123" required minlength="3" maxlength="30">
            </div>
            
            <div>
                <label for="email" class="block text-sm font-medium text-zinc-300 mb-1">Email</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="email" name="email" id="email" placeholder="your@example.com" required>
            </div>
            
            <div>
                <label for="age" class="block text-sm font-medium text-zinc-300 mb-1">Age</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="number" name="age" id="age" placeholder="e.g., 25" min="0" max="120" required>
            </div>
            
            <div>
                <label for="password" class="block text-sm font-medium text-zinc-300 mb-1">Password</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="password" name="password" id="password" placeholder="********" required minlength="6">
            </div>
            
            <input class="mt-6 bg-green-600 px-6 py-3 rounded-lg text-white font-semibold hover:bg-green-700 transition-colors cursor-pointer" 
                   type="submit" value="Create User">
        </form>
        
        <div class="text-center mt-6 pt-4 border-t border-zinc-700">
            <p class="text-zinc-400 mb-2">Already have an account?</p>
            <a class="text-blue-500 hover:text-blue-400 transition-colors font-semibold" href="/login">Log in</a>
        </div>
    </div>
</body>
</html>
