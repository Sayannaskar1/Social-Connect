<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-full  text-white ">
    <h1 class="text-2xl font-bold m-4 p-2">Log in </h1>
   <div class=" p-4 m-14 " >
    <form class="flex flex-col" action="/login" method="post" autocomplete="off">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="text-red-500 mx-auto mb-2"><%= error %></div>
        <% } %>
        <!-- CSRF token placeholder, add server-side implementation if using csurf -->
        <% if (typeof csrfToken !== 'undefined') { %>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <% } %>
        <label for="email" class="mx-auto w-1/4 m-3">Email</label>
        <input class="border-[1.5px] border-zinc-500 outline-none inline-block mx-auto w-1/4 m-3 py-2 px-6 rounded-lg bg-zinc-700" type="email" name="email" id="email" placeholder="email" required>
        <label for="password" class="mx-auto w-1/4 m-3">Password</label>
        <input class="border-[1.5px] border-zinc-500 outline-none inline-block mx-auto w-1/4 m-3 py-2 px-6 rounded-lg bg-zinc-700" type="password" name="password" id="password" placeholder="password" required minlength="6">
        <input class="inline-block mx-auto bg-green-600 px-3 w-28 py-2 rounded-lg m-8 text-md" type="submit" value="log in">
    </form>
    <div class="flex flex-col "> <hr  class="w-1/4 block mx-auto" >
        <p class=" w-32  mx-auto mt-6  mb-4">not a member?</p>
        <a class=" w-32 text-blue-800  mx-auto my-1"  href="/">Create account</a>
    </div>
   
   </div>
</body>
</htm