<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex flex-col items-center justify-center text-white font-sans">
    <h1 class="text-4xl font-bold mb-8 text-blue-400">Log in</h1>
    <div class="p-8 bg-zinc-800 rounded-lg shadow-xl w-full max-w-sm border border-zinc-700">
        <form class="flex flex-col gap-4" action="/login" method="post" autocomplete="off">
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="text-red-500 text-center mb-2"><%= error %></div>
            <% } %>
            <% if (typeof csrfToken !== 'undefined') { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>
            
            <div>
                <label for="email" class="block text-sm font-medium text-zinc-300 mb-1">Email</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="email" name="email" id="email" placeholder="your@example.com" required>
            </div>
            
            <div>
                <label for="password" class="block text-sm font-medium text-zinc-300 mb-1">Password</label>
                <input class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none text-white placeholder-zinc-500 focus:border-blue-500 focus:ring-1 focus:ring-blue-500" 
                       type="password" name="password" id="password" placeholder="********" required minlength="6">
            </div>
            
            <input class="mt-6 bg-green-600 px-6 py-3 rounded-lg text-white font-semibold hover:bg-green-700 transition-colors cursor-pointer" 
                   type="submit" value="Log In">
        </form>
        
        <div class="text-center mt-6 pt-4 border-t border-zinc-700">
            <p class="text-zinc-400 mb-2">Not a member?</p>
            <a class="text-blue-500 hover:text-blue-400 transition-colors font-semibold" href="/">Create account</a>
        </div>
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* Light, subtle gradient background */
            background: linear-gradient(135deg, #f0f2f5 0%, #e0e4eb 100%);
            color: #333; /* Default text color for light mode */
        }
        /* Custom scrollbar for a sleek look in light mode */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #e0e4eb;
        }
        ::-webkit-scrollbar-thumb {
            background: #a0aec0;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #718096;
        }
    </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-center">
    <h1 class="text-5xl font-extrabold mb-10 text-blue-600 drop-shadow-lg">Welcome Back!</h1>
    <div class="p-8 bg-white rounded-xl shadow-2xl w-full max-w-xs sm:max-w-sm border border-gray-200 transform hover:scale-105 transition-transform duration-300 ease-in-out">
        <h2 class="text-3xl font-bold mb-6 text-center text-gray-800">Log In</h2>
        <form class="flex flex-col gap-4" action="/login" method="post" autocomplete="off">
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg text-center text-sm animate-pulse"><%= error %></div>
            <% } %>
            <% if (typeof csrfToken !== 'undefined') { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>
            
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
                <input class="w-full px-5 py-2 rounded-lg bg-gray-100 border border-gray-300 outline-none text-gray-800 placeholder-gray-500 focus:border-blue-500 focus:ring-2 focus:ring-blue-500 transition-all duration-200" 
                       type="email" name="email" id="email" placeholder="your@example.com" required>
            </div>
            
            <div>
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                <input class="w-full px-5 py-2 rounded-lg bg-gray-100 border border-gray-300 outline-none text-gray-800 placeholder-gray-500 focus:border-blue-500 focus:ring-2 focus:ring-blue-500 transition-all duration-200" 
                       type="password" name="password" id="password" placeholder="••••••••" required minlength="6">
            </div>
            
            <button type="submit" class="mt-4 bg-gradient-to-r from-green-500 to-green-600 px-8 py-3 rounded-lg text-white font-bold text-lg shadow-lg hover:from-green-600 hover:to-green-700 transition-all duration-300 transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50">
                Log In
            </button>
        </form>
        
        <div class="relative flex items-center justify-center my-6">
            <div class="absolute inset-0 flex items-center">
                <span class="w-full border-t border-gray-300"></span>
            </div>
            <div class="relative bg-white px-4 text-sm text-gray-500">
                Or continue with
            </div>
        </div>

        <!-- Login with Google Button -->
        <form action="/auth/google" method="get">
            <button type="submit" class="w-full flex items-center justify-center px-5 py-2 rounded-lg bg-white text-gray-700 font-semibold shadow-md hover:bg-gray-100 transition-colors duration-200 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">
                <!-- Google SVG Icon -->
                <svg class="w-6 h-6 mr-3" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12.24 10.285V11.85h4.785c-.09 1.875-1.815 3.18-4.785 3.18-2.925 0-5.325-2.385-5.325-5.325S9.315 4.485 12.24 4.485c1.47 0 2.67.615 3.56 1.495l1.17-1.17c-.96-1.02-2.22-1.74-4.73-1.74C7.885 3.07 4.5 6.455 4.5 10.5c0 4.045 3.385 7.43 7.74 7.43 4.5 0 7.56-3.15 7.56-7.29 0-.525-.045-1.05-.15-1.545H12.24z" fill="#EA4335"/>
                    <path d="M4.5 10.5c0-1.275.345-2.475.945-3.51L2.85 5.565C1.905 7.215 1.5 8.79 1.5 10.5c0 1.71.405 3.285 1.35 4.935l2.64-2.055c-.6-1.035-.945-2.235-.945-3.51z" fill="#FBBC04"/>
                    <path d="M12.24 18.005c-2.43 0-4.44-1.29-5.55-3.255l-2.64 2.055c1.47 2.37 4.17 3.99 8.19 3.99 4.965 0 8.355-3.795 8.355-7.725 0-.525-.045-1.05-.15-1.545H12.24v-3.09h7.71c.21 1.11.33 2.31.33 3.63 0 4.77-3.285 8.565-8.04 8.565z" fill="#4285F4"/>
                    <path d="M12.24 4.485c1.47 0 2.67.615 3.56 1.495l1.17-1.17c-.96-1.02-2.22-1.74-4.73-1.74C7.885 3.07 4.5 6.455 4.5 10.5c0 4.045 3.385 7.43 7.74 7.43 4.5 0 7.56-3.15 7.56-7.29 0-.525-.045-1.05-.15-1.545H12.24z" fill="#34A853"/>
                </svg>
                Login with Google
            </button>
        </form>
        
        <div class="text-center mt-6 pt-4 border-t border-gray-300">
            <p class="text-gray-600 mb-2 text-sm">Don't have an account?</p>
            <a class="text-blue-600 hover:text-blue-500 transition-colors font-semibold text-base tracking-wide" href="/">Create a new account</a>
        </div>
    </div>
</body>
</html>

